{
  "name": "@neftlix/desktop",
  "private": true,
  "version": "0.3.0",
  "description": "Neftlix desktop app",
  "type": "module",
  "main": "dist/main.js",
  "productName": "Neftlix",
  "author": "c4rtical",
  "license": "MIT",
  "scripts": {
    "build:server": "cd .. && npm run build --workspace=server",
    "build:web": "cd .. && node -e \"require('node:fs').rmSync('web/dist',{recursive:true,force:true})\" && npm run build --workspace=web",
    "build:main": "tsc -p . && tsc -p tsconfig.preload.json",
    "prepare:dist": "node scripts/prepare.mjs",
    "build": "npm run build:server && npm run build:web && npm run build:main && npm run prepare:dist",
    "check": "tsc --noEmit -p . && tsc --noEmit -p tsconfig.preload.json",
    "test": "node --test --no-warnings=ExperimentalWarning 'test/**/*.test.ts'",
    "dev": "npm run build && electron .",
    "dist": "npm run build && electron-builder --publish never",
    "release": "npm run build && electron-builder --publish always"
  },
  "dependencies": {
    "@fastify/static": "^10.1.3",
    "electron-updater": "^6.8.0",
    "fastify": "^5.0.0"
  },
  "devDependencies": {
    "electron": "^44.3.0",
    "electron-builder": "^26.0.0",
    "typescript": "^5.8.0"
  }
}
