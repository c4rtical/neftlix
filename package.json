{
  "name": "neftlix",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=server & npm run dev --workspace=web & wait",
    "dev:server": "npm run dev --workspace=server",
    "dev:web": "npm run dev --workspace=web",
    "build": "npm run build --workspace=web",
    "start": "npm run build && npm run start --workspace=server",
    "check": "npm run check --workspace=server && npm run check --workspace=web",
    "test": "npm run test --workspace=server"
  },
  "engines": {
    "node": ">=23.6"
  },
  "description": "Netflix-style client for the IPTV (Xtream Codes) provider you already have: movies, series, live TV and sport.",
  "repository": {
    "type": "git",
    "url": "https://github.com/c4rtical/neftlix.git"
  },
  "license": "MIT"
}
